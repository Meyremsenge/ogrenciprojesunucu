# Öğrenci Koçluk Sistemi - Yüksek Seviyeli Sistem Mimarisi
# Lead Backend Engineer Perspektifi

## 📋 İçindekiler

1. [Sistem Genel Bakışı](#sistem-genel-bakışı)
2. [Backend Mimarisi (Flask)](#backend-mimarisi-flask)
3. [PostgreSQL Veritabanı](#postgresql-veritabanı)
4. [Redis Cache ve Oturum Yönetimi](#redis-cache-ve-oturum-yönetimi)
5. [Celery Arka Plan Görevleri](#celery-arka-plan-görevleri)
6. [Nginx + Gunicorn Yapısı](#nginx--gunicorn-yapısı)
7. [YouTube CDN Video Stratejisi](#youtube-cdn-video-stratejisi)
8. [10.000+ Kullanıcı için Mimari Kararlar](#10000-kullanıcı-için-mimari-kararlar)

---

## Sistem Genel Bakışı

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              INTERNET / CLIENTS                                  │
│                    (Web Browsers, Mobile Apps, API Consumers)                   │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CLOUDFLARE CDN                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │ DDoS Shield │  │  WAF Rules  │  │ SSL/TLS    │  │ Static CDN  │            │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           LOAD BALANCER (HAProxy/ALB)                           │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Health Checks │ Round Robin │ Sticky Sessions │ SSL Termination       │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
                    │                    │                    │
                    ▼                    ▼                    ▼
┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
│    NGINX + GUNICORN   │  │    NGINX + GUNICORN   │  │    NGINX + GUNICORN   │
│      Instance 1       │  │      Instance 2       │  │      Instance N       │
│  ┌─────────────────┐  │  │  ┌─────────────────┐  │  │  ┌─────────────────┐  │
│  │  Nginx (proxy)  │  │  │  │  Nginx (proxy)  │  │  │  │  Nginx (proxy)  │  │
│  └────────┬────────┘  │  │  └────────┬────────┘  │  │  └────────┬────────┘  │
│           ▼           │  │           ▼           │  │           ▼           │
│  ┌─────────────────┐  │  │  ┌─────────────────┐  │  │  ┌─────────────────┐  │
│  │ Gunicorn Master │  │  │  │ Gunicorn Master │  │  │  │ Gunicorn Master │  │
│  │   (4 workers)   │  │  │  │   (4 workers)   │  │  │  │   (4 workers)   │  │
│  │  ┌───┬───┬───┐  │  │  │  │  ┌───┬───┬───┐  │  │  │  │  ┌───┬───┬───┐  │  │
│  │  │W1 │W2 │W3 │W4│  │  │  │  │W1 │W2 │W3 │W4│  │  │  │  │W1 │W2 │W3 │W4│  │
│  │  └───┴───┴───┘  │  │  │  │  └───┴───┴───┘  │  │  │  │  └───┴───┴───┘  │  │
│  └─────────────────┘  │  │  └─────────────────┘  │  │  └─────────────────┘  │
│         FLASK         │  │         FLASK         │  │         FLASK         │
└───────────────────────┘  └───────────────────────┘  └───────────────────────┘
           │                         │                         │
           └─────────────────────────┼─────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
┌───────────────────┐    ┌───────────────────────┐    ┌───────────────────┐
│   REDIS CLUSTER   │    │  POSTGRESQL CLUSTER   │    │   CELERY CLUSTER  │
│                   │    │                       │    │                   │
│  ┌─────────────┐  │    │  ┌─────────────────┐  │    │  ┌─────────────┐  │
│  │   Master    │  │    │  │  Primary (R/W)  │  │    │  │   Worker 1  │  │
│  └──────┬──────┘  │    │  └────────┬────────┘  │    │  └─────────────┘  │
│         │         │    │           │           │    │  ┌─────────────┐  │
│  ┌──────┴──────┐  │    │  ┌────────┴────────┐  │    │  │   Worker 2  │  │
│  │   Replica   │  │    │  │  Replica (R/O)  │  │    │  └─────────────┘  │
│  └─────────────┘  │    │  └─────────────────┘  │    │  ┌─────────────┐  │
│                   │    │  ┌─────────────────┐  │    │  │   Beat      │  │
│  Cache + Sessions │    │  │  Replica (R/O)  │  │    │  │  (Scheduler)│  │
│  + Rate Limiting  │    │  └─────────────────┘  │    │  └─────────────┘  │
└───────────────────┘    └───────────────────────┘    └───────────────────┘
                                     │
                                     ▼
                         ┌───────────────────────┐
                         │     PGBOUNCER         │
                         │  (Connection Pooler)  │
                         └───────────────────────┘

                                     │
    ┌────────────────────────────────┼────────────────────────────┐
    │                                │                            │
    ▼                                ▼                            ▼
┌───────────────────┐    ┌───────────────────────┐    ┌───────────────────┐
│   YOUTUBE CDN     │    │    OBJECT STORAGE     │    │    MONITORING     │
│                   │    │     (MinIO/S3)        │    │                   │
│  Video Streaming  │    │  Documents, Images    │    │  Prometheus +     │
│  Unlisted Videos  │    │  Exports, Backups     │    │  Grafana + Loki   │
└───────────────────┘    └───────────────────────┘    └───────────────────┘
```

---

## Backend Mimarisi (Flask)

### Mimari Kararlar ve Gerekçeler

#### 1. Application Factory Pattern
```
app/
├── __init__.py          # create_app() factory
├── config.py            # Environment-based configuration
├── extensions.py        # Flask extension initialization
├── api/                 # REST API endpoints
│   ├── v1/              # API versioning
│   │   ├── __init__.py  # Blueprint + Flask-RESTX
│   │   ├── auth.py      # Authentication endpoints
│   │   ├── users.py     # User management
│   │   ├── courses.py   # Course operations
│   │   └── ...
│   └── decorators.py    # Permission decorators
├── models/              # SQLAlchemy models
├── services/            # Business logic layer
├── tasks/               # Celery background tasks
├── utils/               # Helper utilities
└── schemas/             # Marshmallow serializers
```

**Gerekçe:**
- **Testability**: Application factory pattern ile farklı config'lerle test instance'ları oluşturulabilir
- **Flexibility**: Her deployment için farklı konfigürasyon (dev/test/prod)
- **Circular Import Prevention**: Extensions ayrı modülde, lazy initialization

#### 2. Layered Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                      │
│                  (Flask-RESTX API Endpoints)                │
│         Request validation, Response formatting             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       SERVICE LAYER                          │
│                    (Business Logic)                          │
│    Authentication, Authorization, Domain operations         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATA ACCESS LAYER                         │
│                  (SQLAlchemy Models)                         │
│            Database operations, Query building               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   INFRASTRUCTURE LAYER                       │
│            PostgreSQL, Redis, External APIs                  │
└─────────────────────────────────────────────────────────────┘
```

**Gerekçe:**
- **Separation of Concerns**: Her katman tek sorumluluğa sahip
- **Maintainability**: Değişiklikler izole, etki alanı sınırlı
- **Testability**: Her katman bağımsız test edilebilir

---

## PostgreSQL Veritabanı

### Database Schema Design

```sql
-- Core Tables Relationship Diagram

┌─────────────────┐       ┌─────────────────┐
│     users       │       │     roles       │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │───────│ id (PK)         │
│ email           │       │ name            │
│ password_hash   │       │ permissions     │
│ role_id (FK)    │       └─────────────────┘
│ is_active       │
└────────┬────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐       ┌─────────────────┐
│   enrollments   │       │    courses      │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │───────│ id (PK)         │
│ user_id (FK)    │       │ title           │
│ course_id (FK)  │       │ teacher_id (FK) │
│ status          │       │ category_id     │
│ enrolled_at     │       │ is_published    │
└─────────────────┘       └────────┬────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │              │              │
                    ▼              ▼              ▼
           ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
           │   topics    │ │   videos    │ │    exams    │
           ├─────────────┤ ├─────────────┤ ├─────────────┤
           │ id (PK)     │ │ id (PK)     │ │ id (PK)     │
           │ course_id   │ │ course_id   │ │ course_id   │
           │ title       │ │ youtube_id  │ │ time_limit  │
           │ order_index │ │ duration    │ │ questions   │
           └─────────────┘ └─────────────┘ └─────────────┘
```

### Connection Pooling Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION SERVERS                           │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐    │
│  │  Flask 1  │  │  Flask 2  │  │  Flask 3  │  │  Flask 4  │    │
│  │ Pool: 20  │  │ Pool: 20  │  │ Pool: 20  │  │ Pool: 20  │    │
│  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘    │
└────────┼──────────────┼──────────────┼──────────────┼───────────┘
         │              │              │              │
         └──────────────┴──────┬───────┴──────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │     PGBOUNCER       │
                    │  ┌───────────────┐  │
                    │  │ Pool Mode:    │  │
                    │  │ Transaction   │  │
                    │  │ Max: 100      │  │
                    │  │ Default: 20   │  │
                    │  └───────────────┘  │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │    POSTGRESQL       │
                    │  max_connections:   │
                    │       200           │
                    └─────────────────────┘
```

**Gerekçe:**
- **PgBouncer**: Connection overhead'i azaltır, PostgreSQL bağlantı limitini korur
- **Transaction Pooling**: Her query için connection almak yerine transaction bazlı
- **10K kullanıcı için**: 4 app server × 20 connection = 80 aktif, PgBouncer ile 100'e sıkıştırılır

### Indexing Strategy

```sql
-- Critical Indexes for Performance

-- User lookups (login, search)
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_active ON users(is_active) WHERE is_active = true;

-- Enrollment queries
CREATE INDEX idx_enrollments_user_course ON enrollments(user_id, course_id);
CREATE INDEX idx_enrollments_status ON enrollments(status);

-- Course filtering
CREATE INDEX idx_courses_published ON courses(is_published, is_active);
CREATE INDEX idx_courses_category ON courses(category_id);
CREATE INDEX idx_courses_teacher ON courses(teacher_id);

-- Video progress tracking (heavy read)
CREATE INDEX idx_video_progress_user_video ON video_progress(user_id, video_id);

-- Exam results (reporting)
CREATE INDEX idx_exam_results_user ON exam_results(user_id, exam_id);
CREATE INDEX idx_exam_results_date ON exam_results(submitted_at);

-- Full-text search (optional)
CREATE INDEX idx_courses_search ON courses USING gin(to_tsvector('turkish', title || ' ' || description));
```

---

## Redis Cache ve Oturum Yönetimi

### Redis Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       REDIS CLUSTER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │    Database 0   │  │    Database 1   │  │    Database 2   │ │
│  │   API Cache     │  │    Sessions     │  │  Rate Limiting  │ │
│  │                 │  │                 │  │                 │ │
│  │ - Course lists  │  │ - JWT blacklist │  │ - IP counters   │ │
│  │ - User profiles │  │ - Active tokens │  │ - User quotas   │ │
│  │ - Video meta    │  │ - Login state   │  │ - API limits    │ │
│  │                 │  │                 │  │                 │ │
│  │ TTL: 5-60 min   │  │ TTL: Token exp  │  │ TTL: 1 min      │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Cache Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                    CACHE-ASIDE PATTERN                           │
└─────────────────────────────────────────────────────────────────┘

   Request                                         Response
      │                                                ▲
      ▼                                                │
┌───────────┐   Cache Hit?   ┌───────────┐            │
│   Flask   │──────YES──────▶│   Redis   │────────────┤
│    App    │                └───────────┘            │
└─────┬─────┘                                         │
      │                                               │
      NO                                              │
      │                                               │
      ▼                                               │
┌───────────┐                ┌───────────┐            │
│ PostgreSQL│───Data────────▶│   Redis   │            │
└───────────┘     │          │  (Cache)  │            │
                  │          └───────────┘            │
                  │                                   │
                  └───────────────────────────────────┘

Cache Invalidation:
- Write-through: Update cache on write
- TTL-based: Auto-expire stale data
- Event-based: Invalidate on specific events
```

### Key Design Pattern

```
# Cache Key Naming Convention
# {app}:{resource}:{identifier}:{variant}

# Examples:
sc:user:123:profile          # User profile
sc:course:456:detail         # Course detail
sc:course:456:videos         # Course videos list
sc:courses:list:page:1       # Paginated course list
sc:user:123:enrollments      # User's enrollments
sc:stats:daily:2024-12-24    # Daily statistics

# Session Keys
session:jwt:blacklist:{jti}  # Revoked tokens
session:user:123:active      # Active session flag

# Rate Limiting Keys
ratelimit:ip:192.168.1.1     # IP-based limit
ratelimit:user:123:api       # User API limit
```

---

## Celery Arka Plan Görevleri

### Task Queue Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      CELERY ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │   Flask App     │
                    │  (Producer)     │
                    └────────┬────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     REDIS (Message Broker)                       │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐         │
│  │ celery:high   │ │ celery:default│ │ celery:low    │         │
│  │ (Priority 10) │ │ (Priority 5)  │ │ (Priority 1)  │         │
│  └───────────────┘ └───────────────┘ └───────────────┘         │
└─────────────────────────────────────────────────────────────────┘
         │                   │                   │
         ▼                   ▼                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CELERY WORKERS                              │
│                                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  Worker 1   │  │  Worker 2   │  │  Worker 3   │             │
│  │ Concurrency │  │ Concurrency │  │ Concurrency │             │
│  │     4       │  │     4       │  │     4       │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                  │
│  Task Types:                                                     │
│  ├── Email sending (async)                                       │
│  ├── Report generation                                           │
│  ├── Video metadata sync                                         │
│  ├── Notification dispatch                                       │
│  └── Data aggregation                                            │
│                                                                  │
│  ┌─────────────┐                                                │
│  │ Celery Beat │  Scheduled Tasks:                              │
│  │ (Scheduler) │  ├── Daily reports (02:00)                     │
│  └─────────────┘  ├── Cache warmup (hourly)                     │
│                   ├── Cleanup old sessions (daily)               │
│                   └── Video stats sync (every 6h)                │
└─────────────────────────────────────────────────────────────────┘
```

### Task Prioritization

```python
# Queue Priority Mapping
CELERY_TASK_ROUTES = {
    # High Priority - User-facing, time-sensitive
    'app.tasks.email_tasks.send_password_reset_email_task': {'queue': 'high'},
    'app.tasks.email_tasks.send_verification_email_task': {'queue': 'high'},
    
    # Default Priority - Standard operations
    'app.tasks.email_tasks.send_welcome_email_task': {'queue': 'default'},
    'app.tasks.video_tasks.fetch_video_metadata_task': {'queue': 'default'},
    
    # Low Priority - Background maintenance
    'app.tasks.report_tasks.generate_weekly_summary_task': {'queue': 'low'},
    'app.tasks.video_tasks.sync_all_video_metadata_task': {'queue': 'low'},
}
```

---

## Nginx + Gunicorn Yapısı

### Request Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        REQUEST FLOW                              │
└─────────────────────────────────────────────────────────────────┘

Client Request
      │
      ▼
┌───────────────────────────────────────────────────────────────┐
│                         NGINX                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Connection Handling                   │  │
│  │  • Accept connections (10,000 worker_connections)       │  │
│  │  • SSL/TLS termination                                  │  │
│  │  • HTTP/2 support                                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Request Processing                    │  │
│  │  • Rate limiting (100 req/s per IP)                     │  │
│  │  • Request buffering                                    │  │
│  │  • Gzip compression                                     │  │
│  │  • Static file serving                                  │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Proxy to Upstream                     │  │
│  │  • Load balancing (least_conn)                          │  │
│  │  • Health checks                                        │  │
│  │  • Keepalive connections                                │  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌───────────────────────────────────────────────────────────────┐
│                        GUNICORN                                │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Master Process                        │  │
│  │  • Worker management                                    │  │
│  │  • Signal handling                                      │  │
│  │  • Graceful restarts                                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                 │
│  ┌───────────┬───────────┬───────────┬───────────┐           │
│  │ Worker 1  │ Worker 2  │ Worker 3  │ Worker 4  │           │
│  │ (gevent)  │ (gevent)  │ (gevent)  │ (gevent)  │           │
│  │  1000     │  1000     │  1000     │  1000     │           │
│  │  conns    │  conns    │  conns    │  conns    │           │
│  └───────────┴───────────┴───────────┴───────────┘           │
│                                                                │
│  Total Capacity per Instance: 4,000 concurrent connections    │
│  With 4 instances: 16,000 concurrent connections              │
└───────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌───────────────────────────────────────────────────────────────┐
│                      FLASK APPLICATION                         │
│  • Process request                                             │
│  • Query database (via connection pool)                        │
│  • Check/update cache                                          │
│  • Return response                                             │
└───────────────────────────────────────────────────────────────┘
```

### Gunicorn Worker Calculation

```
┌─────────────────────────────────────────────────────────────────┐
│                  WORKER SIZING FORMULA                           │
└─────────────────────────────────────────────────────────────────┘

For CPU-bound tasks:
  workers = (2 × CPU_CORES) + 1
  
For I/O-bound tasks (our case - DB, Redis, APIs):
  workers = (2 × CPU_CORES) + 1
  worker_class = 'gevent'  # Async I/O
  worker_connections = 1000  # Per worker

Example (4-core server):
  workers = (2 × 4) + 1 = 9 workers
  With gevent: 9 × 1000 = 9,000 concurrent connections
  
Recommended settings:
  workers = 4 (to leave CPU headroom)
  threads = 2 (hybrid approach)
  worker_connections = 1000

Memory calculation:
  Each worker ≈ 200-300 MB
  4 workers × 300 MB = 1.2 GB RAM for Gunicorn
  Buffer for spikes: 2 GB recommended
```

---

## YouTube CDN Video Stratejisi

### Video Delivery Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    VIDEO DELIVERY FLOW                           │
└─────────────────────────────────────────────────────────────────┘

                         ┌─────────────┐
                         │   Teacher   │
                         │  (Upload)   │
                         └──────┬──────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   YouTube Studio      │
                    │  (Direct Upload)      │
                    │  Privacy: Unlisted    │
                    └───────────┬───────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ▼                       ▼
         ┌─────────────────┐    ┌─────────────────┐
         │  YouTube CDN    │    │  Our Database   │
         │ (Video Storage) │    │  (Metadata)     │
         │                 │    │                 │
         │ • Transcoding   │    │ • video_id      │
         │ • Multi-quality │    │ • youtube_id    │
         │ • Global CDN    │    │ • title         │
         │ • Adaptive      │    │ • course_id     │
         │   streaming     │    │ • order_index   │
         └────────┬────────┘    └────────┬────────┘
                  │                      │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │    Student Views    │
                  │      Video          │
                  └──────────┬──────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ Embed Check   │  │ Video Player  │  │ Progress      │
│ (Our Server)  │  │ (YouTube)     │  │ Tracking      │
│               │  │               │  │ (Our Server)  │
│ • Auth check  │  │ • Streaming   │  │               │
│ • Enrollment  │  │ • Quality     │  │ • Watched %   │
│ • Access log  │  │ • Buffering   │  │ • Last pos    │
└───────────────┘  └───────────────┘  └───────────────┘
```

### Embed Security Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              SECURE VIDEO EMBED FLOW                             │
└─────────────────────────────────────────────────────────────────┘

1. Student requests video page
   │
   ▼
2. Server checks:
   ├── Is user authenticated? (JWT)
   ├── Is user enrolled in course?
   └── Is video part of accessible content?
   │
   ▼
3. If authorized:
   │
   ▼
4. Generate embed response:
   {
     "video_id": 123,
     "embed_url": "https://www.youtube.com/embed/VIDEO_ID?rel=0&modestbranding=1",
     "can_access": true
   }
   │
   ▼
5. Frontend renders YouTube iframe
   │
   ▼
6. Video streams directly from YouTube CDN
   (No load on our servers!)
   │
   ▼
7. Progress updates sent to our API
   POST /api/v1/videos/123/progress
   {
     "watched_seconds": 450,
     "total_seconds": 1200
   }
```

**Gerekçeler:**
- **Bandwidth Cost**: YouTube CDN bedava, kendi video sunmak çok pahalı
- **Scalability**: YouTube global CDN, bizim sunuculara yük binmez
- **Quality**: Adaptive bitrate, her bağlantı hızına uygun
- **Unlisted**: Video linkini bilen izleyebilir ama arama sonuçlarında çıkmaz

---

## 10.000+ Kullanıcı için Mimari Kararlar

### Capacity Planning

```
┌─────────────────────────────────────────────────────────────────┐
│                    CAPACITY CALCULATION                          │
└─────────────────────────────────────────────────────────────────┘

Target: 10,000 concurrent users

User Activity Distribution (typical):
├── 60% watching videos      → YouTube CDN (no server load)
├── 25% browsing/reading     → Cached content (Redis hit)
├── 10% taking exams         → Database read/write
└── 5% heavy operations      → Full server processing

Actual Server Load:
├── Video watchers: 0 RPS (YouTube handles)
├── Cache hits: 2,500 users × 0.5 RPS = 1,250 RPS (Redis)
├── Exam takers: 1,000 users × 2 RPS = 2,000 RPS
└── Heavy ops: 500 users × 1 RPS = 500 RPS

Peak API Load: ~3,750 RPS
With 4× safety margin: Target 15,000 RPS capacity
```

### Infrastructure Sizing

```
┌─────────────────────────────────────────────────────────────────┐
│              RECOMMENDED INFRASTRUCTURE                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ COMPONENT          │ QUANTITY │ SPECS              │ PURPOSE    │
├────────────────────┼──────────┼────────────────────┼────────────┤
│ Load Balancer      │    2     │ 2 vCPU, 4GB RAM   │ HA + SSL   │
│ App Servers        │    4     │ 4 vCPU, 8GB RAM   │ 4K conn/ea │
│ PostgreSQL Primary │    1     │ 8 vCPU, 32GB RAM  │ Writes     │
│ PostgreSQL Replica │    2     │ 4 vCPU, 16GB RAM  │ Reads      │
│ Redis Primary      │    1     │ 2 vCPU, 8GB RAM   │ Cache      │
│ Redis Replica      │    1     │ 2 vCPU, 8GB RAM   │ Failover   │
│ Celery Workers     │    2     │ 2 vCPU, 4GB RAM   │ Async jobs │
│ Monitoring         │    1     │ 2 vCPU, 4GB RAM   │ Prometheus │
└─────────────────────────────────────────────────────────────────┘

Total Resources:
├── vCPUs: 42
├── RAM: 112 GB
├── Storage: ~1 TB SSD
└── Estimated Monthly Cost: $800-1,500 (cloud)
```

### Bottleneck Prevention

```
┌─────────────────────────────────────────────────────────────────┐
│                 BOTTLENECK ANALYSIS                              │
└─────────────────────────────────────────────────────────────────┘

Potential Bottleneck          Solution Implemented
─────────────────────────────────────────────────────────────────
1. Database Connections       → PgBouncer connection pooling
                              → Read replicas for read-heavy queries
                              → Proper indexing

2. API Response Time          → Redis caching (5-60 min TTL)
                              → Query optimization
                              → Pagination enforced

3. Video Bandwidth            → YouTube CDN (offload 100%)
                              → No server-side video processing

4. Memory Usage               → Gevent workers (async I/O)
                              → Lazy loading relationships
                              → Streaming responses for large data

5. Single Point of Failure    → Multiple app instances
                              → Database replication
                              → Redis replica
                              → Load balancer HA pair

6. Slow Background Tasks      → Celery with priority queues
                              → Task timeouts
                              → Dead letter queue for failures

7. Rate Limiting              → Redis-based token bucket
                              → Per-IP and per-user limits
                              → Nginx layer protection
```

### Auto-Scaling Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTO-SCALING RULES                            │
└─────────────────────────────────────────────────────────────────┘

                         Metrics Monitored
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
   CPU Usage             Response Time          Queue Depth
   (Gunicorn)            (Nginx p95)            (Celery)
        │                      │                      │
        ▼                      ▼                      ▼
   ┌─────────┐            ┌─────────┐            ┌─────────┐
   │ > 70%   │            │ > 500ms │            │ > 1000  │
   │ for 5m  │            │ for 5m  │            │ for 5m  │
   └────┬────┘            └────┬────┘            └────┬────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │   SCALE UP          │
                    │   +2 app instances  │
                    │   +1 celery worker  │
                    └─────────────────────┘

                         Cool-down Period
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
   CPU Usage             Response Time          Queue Depth
        │                      │                      │
        ▼                      ▼                      ▼
   ┌─────────┐            ┌─────────┐            ┌─────────┐
   │ < 30%   │            │ < 100ms │            │ < 100   │
   │ for 15m │            │ for 15m │            │ for 15m │
   └────┬────┘            └────┬────┘            └────┬────┘
        │                      │                      │
        └──────────────────────┼──────────────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │   SCALE DOWN        │
                    │   -1 app instance   │
                    │   (min: 4)          │
                    └─────────────────────┘
```

---

## Özet: Kritik Mimari Kararlar

| Karar | Gerekçe | Etki |
|-------|---------|------|
| **Flask + Gunicorn + Gevent** | Async I/O için ideal, 1000+ conn/worker | 4 instance ile 16K concurrent |
| **PgBouncer** | Connection pooling, DB bağlantı limitini korur | 80% connection tasarrufu |
| **Read Replicas** | Okuma yükünü dağıt | 3x read capacity |
| **Redis Cache** | Sık erişilen data memory'de | 90%+ cache hit ratio |
| **YouTube CDN** | Video sunmak pahalı, YouTube bedava | 0 bandwidth cost |
| **Celery Queues** | Sync işlemler async'e taşındı | API response time < 100ms |
| **Nginx Rate Limit** | DDoS ve abuse koruması | Sistem stabilitesi |
| **Horizontal Scaling** | Yatay ölçekleme mümkün | 10K → 100K kolayca |

---

*Doküman: Lead Backend Engineer - Öğrenci Koçluk Sistemi*
*Versiyon: 1.0*
*Tarih: 24 Aralık 2025*
